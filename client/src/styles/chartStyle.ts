import styled from "styled-components";

export const ChartContainer = styled.div`
  border: 0.5px solid ${(props) => props.theme.periwinkleShade90Faint};
  border-radius: 20px;
  margin-top: 20px;
  padding-top: 20px;
`;
